info:
  name: Exception Detail
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/projects/{{project}}/exceptions/{{exceptionId}}"
  headers:
    - name: Accept
      value: application/json
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Dettaglio eccezione con contesto prima/dopo e riferimento al file principale.
