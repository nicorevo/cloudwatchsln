# Installa dipendenze
sudo pacman -S curl unzip

# Scarica e installa AWS CLI v2
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

# Verifica
aws --version

# Cleanup
rm -rf aws awscliv2.zip

aws sso login --profile revo-uat
